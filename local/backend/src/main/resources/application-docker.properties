spring.datasource.url=jdbc:postgresql://postgres:5432/cvd
spring.datasource.username=postgres
spring.datasource.password=postgres
ml.service.url=http://cvd-ml:5001

# Secrets injected via Docker environment variables
ml.api.key=${CVD_ML_API_KEY}
jwt.secret=${CVD_JWT_SECRET}
